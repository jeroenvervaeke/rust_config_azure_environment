[package]
name = "rust_config_azure_environment"
version = "0.1.0"
authors = ["Jeroen Vervaeke <jeroenvervaeke@users.noreply.github.com>"]
edition = "2018"

[dependencies]
config = "0.11"
lazy_static = "1.4"
regex = "1.5"

[dev-dependencies]
serde = { version = "1.0", features = [ "derive" ] }